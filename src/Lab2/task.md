+ Описать иерархию классов (минимум 4). Базовый класс абстрактный с минимум 1 абстрактной функцией
+ Описать class Main с функцией main вне иерархии
+ Описать как минимум 2 интерфейса, каждый подключить минимум в 1 класс иерархии
+ Каждый наследный класс должен иметь уникальные поля и методы
+ Вызвать конструкторы классов родителей с параметрами и без
+ Вызвать обычные функции из классов родителей
+ Переопределить в каждом классе Randomize в дефолтном конструкторе 
+ Переопределить в каждом классе функцию ввода полей с клавиатуры
+ Переопределить в каждом классе функцию записи в файл 						
? Создать минимум одну final : переменную && функцию && класс
? Создать минимум одну static : переменную && функцию
+ Использовать instanceof и преобразование типов объектов
В main создать вектора объектов с типами: базовый класс && интерфейс
+ В функции main для созданных объектов вызвать уникальные функции их классов, вывести их на экран
Описать в отчете собственный вариант работы так, как показано в примерах ниже.
После чего - сказать “УРРРРАААА ! ! ! !” и сдать уже вторую лабораторную работу преподавателю. :)